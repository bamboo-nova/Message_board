<% if current_user.liking?(user_post) %>
  <%= form_for(current_user.favorites.find_by(micropost_id: user_post.id), html: { method: :delete }) do |f| %>
    <%= hidden_field_tag :micropost_id, user_post.id %>
    <%= f.submit 'Unlike', class: 'btn btn-danger btn-xs' %>
  <% end %>
<% else %>
  <%= form_for(current_user.favorites.build) do |f| %>
    <%= hidden_field_tag :micropost_id, user_post.id %>
    <%= f.submit 'Like', class: 'btn btn-primary btn-xs' %>
  <% end %>
<% end %>